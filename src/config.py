from os import path

ROOT_DIR, _ = path.split(__file__)
""" ROOT_DIR is the path of the executable """


DATA_FILE = ROOT_DIR + "/data/task_data.json"
""" DATA_FILE is the path of the file where new tasks are written """

