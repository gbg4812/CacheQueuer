Pseudo code:

```python
#on initialitzation
layout = RailLayout(margins, spacing)
layout.addLItem(remove_butt)
layout.computeLayout()

#paint method:
layout.computeLayout(pos)
layout.setWidth(width)
layout.draw(painter)

```
